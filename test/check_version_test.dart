import 'package:flutter_test/flutter_test.dart';

import 'package:check_version/src/check_version.dart';

void main() {
  test('adds one to input values', () {
    final calculator = CheckVersion();
  });
}
